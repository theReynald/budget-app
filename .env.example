# Copy to .env and adjust values as needed
# Port for Express API
PORT=5055

# Preferred AI model identifier for OpenRouter (choose a supported model)
AI_MODEL=openai/gpt-4o-mini

# Base URL for API calls in production (leave blank for dev proxy)
VITE_API_BASE=http://localhost:5055

# Your OpenRouter API key (DO NOT COMMIT .env). Copy this line into your local .env and replace placeholder.
OPENROUTER_API_KEY=YOUR_OPENROUTER_KEY_HERE

# NOTE: The frontend no longer asks for an API key; the backend reads OPENROUTER_API_KEY from process.env.

# Any additional feature flags
# ENABLE_STREAMING=false
